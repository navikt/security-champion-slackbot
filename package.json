{
  "name": "security-champion-slackbot",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "build/cli.js",
  "scripts": {
    "test": "jest",
    "start": "node build/cli.js",
    "build": "tsc",
    "prepare": "husky",
    "refresh": "rm -rf node_modules && rm -f pnpm-lock.yaml package-lock.json && pnpm install",
    "test-storage": "node build/cli.js test-storage || exit 0",
    "sync": "node build/cli.js sync || exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "@slack/bolt": "^4.7.2",
    "@slack/web-api": "^7.16.0",
    "cross-fetch": "^4.1.0",
    "self-update": "^0.0.1",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.1",
    "husky": "^9.1.7",
    "jest": "^30.4.2",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  },
  "lint-staged": {
    "*.{js,ts,css,md}": "prettier --write"
  }
}
