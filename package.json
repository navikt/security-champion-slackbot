{
  "name": "security-champion-slackbot",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node cli.js",
    "prepare": "husky install",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm i",
    "test-storage": "node cli.js test-storage || exit 0",
    "sync": "node cli.js sync || exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "5.18.2",
    "@slack/bolt": "3.11.3",
    "cross-fetch": "3.1.5",
    "yargs": "17.5.1"
  },
  "devDependencies": {
    "husky": "8.0.1",
    "lint-staged": "13.0.2",
    "prettier": "2.7.1"
  },
  "lint-staged": {
    "*.{js,css,md}": "prettier --write"
  }
}
